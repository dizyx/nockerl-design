---
title: Agent widget
description: The compact agent-run card, with an identity header + the shared lifecycle chip.
status: review
platforms: [android, swift]
---

import StatusBadge from '../../../components/StatusBadge.astro';
import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import goldenDark from '../../../../../packages/compose/nockerl-design-gallery/src/test/screenshots/gallery/dark/agentwidget-lifecycle.png';

<StatusBadge status="review" />

<img src={goldenDark.src} alt="Three agent-run widgets: running with detail, done, and failed" style="border-radius: 12px;" />

The compact "an agent is working" card (WS1 #2652b), a tool-card-family sibling that is
**identity-led** (avatar initials + name + mono model badge) where the
[Spawn block](/components/spawn-cards/) is mechanism-led (family tile). Siblings, not
twins. The **lifecycle chip** (dot + label + elapsed) is the same `NockerlSpawnStatus`
ladder; an optional one-line `detail` carries the current step or last output.

## Code

<Tabs syncKey="lang">
<TabItem label="Kotlin">

```kotlin
NockerlAgentWidget(
    name = "Design Builder 2",
    status = NockerlSpawnStatus.RUNNING,
    model = "large-2",
    elapsed = "2m 40s",
    detail = "Recording goldens for the spawn cards",
)
```

</TabItem>
<TabItem label="Swift">

```swift
NockerlAgentWidget(
    name: "Design Builder 2",
    status: .running,
    model: "large-2",
    elapsed: "2m 40s",
    detail: "Recording goldens for the spawn cards"
)
```

</TabItem>
</Tabs>

## Props

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `name` | `String` | n/a | Display name (drives the avatar initials). |
| `status` | `NockerlSpawnStatus` | n/a | The shared lifecycle ladder. |
| `model` | `String?` | `null` | Mono model badge text. |
| `elapsed` | `String?` | `null` | Elapsed label in the chip. |
| `detail` | `String?` | `null` | One-line status detail under the name. |
| `animate` | `Boolean` | `true` | `false` freezes the RUNNING pulse. |

<Aside type="caution" title="Swift avatar">
  The Swift widget currently renders an interim initials disc. The full
  `NockerlAvatar` (image + presence) is a flagged Swift bring-up; Compose and react
  ship the real avatar today.
</Aside>
